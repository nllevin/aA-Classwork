<h1>99 Cats!</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <a href="<%= cat_url(cat.id) %>">
        <%= cat.name %>
      </a>
    </li>
  <% end %>
</ul>

<br>

<a href="<%= new_cat_url %>">Add a cat?</a>